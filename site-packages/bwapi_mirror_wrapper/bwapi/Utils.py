from bwapi import Utils as Java_Utils

if False:
    class Utils(object):

        def __init__(self, *args, **kwargs):
            self._base = Java_Utils(*args, **kwargs)

        def __getattribute__(self, attr):
            return getattr(object.__getattribute__(self, "_base"), attr)

        def formatText(self, text, format, ):
            pass
        
else:
    Utils = Java_Utils
