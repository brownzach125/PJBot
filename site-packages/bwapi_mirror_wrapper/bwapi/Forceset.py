from bwapi import Forceset as Java_Forceset

if False:
    class Forceset(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_Forceset, field_name)
            return getattr(unit_type, attr)

        def getPlayers(self, ):
            pass
        
else:
    Forceset = Java_Forceset
