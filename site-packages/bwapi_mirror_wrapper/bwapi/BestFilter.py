from bwapi import BestFilter as Java_BestFilter

if False:
    class BestFilter(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_BestFilter, field_name)
            return getattr(unit_type, attr)

else:
    BestFilter = Java_BestFilter
