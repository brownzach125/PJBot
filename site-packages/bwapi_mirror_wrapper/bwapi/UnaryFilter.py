from bwapi import UnaryFilter as Java_UnaryFilter

if False:
    class UnaryFilter(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_UnaryFilter, field_name)
            return getattr(unit_type, attr)

else:
    UnaryFilter = Java_UnaryFilter
