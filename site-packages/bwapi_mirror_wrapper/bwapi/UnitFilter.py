from bwapi import UnitFilter as Java_UnitFilter

if False:
    class UnitFilter(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_UnitFilter, field_name)
            return getattr(unit_type, attr)

else:
    UnitFilter = Java_UnitFilter
