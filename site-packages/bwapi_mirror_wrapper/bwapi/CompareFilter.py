from bwapi import CompareFilter as Java_CompareFilter

if False:
    class CompareFilter(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_CompareFilter, field_name)
            return getattr(unit_type, attr)

else:
    CompareFilter = Java_CompareFilter
