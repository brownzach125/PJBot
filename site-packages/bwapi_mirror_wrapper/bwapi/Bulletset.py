from bwapi import Bulletset as Java_Bulletset

if False:
    class Bulletset(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_Bulletset, field_name)
            return getattr(unit_type, attr)

else:
    Bulletset = Java_Bulletset
