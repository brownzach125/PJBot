from bwapi import BestUnitFilter as Java_BestUnitFilter

if False:
    class BestUnitFilter(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_BestUnitFilter, field_name)
            return getattr(unit_type, attr)

else:
    BestUnitFilter = Java_BestUnitFilter
