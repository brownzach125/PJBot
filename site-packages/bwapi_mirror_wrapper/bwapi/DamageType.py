from bwapi import DamageType as Java_DamageType

if False:
    class DamageType(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_DamageType, field_name)
            return getattr(unit_type, attr)

        def toString(self, ):
            pass
        
        
        Independent = DamageType("Independent")
        
        Explosive = DamageType("Explosive")
        
        Concussive = DamageType("Concussive")
        
        Normal = DamageType("Normal")
        
        Ignore_Armor = DamageType("Ignore_Armor")
        
        Unknown = DamageType("Unknown")
else:
    DamageType = Java_DamageType
