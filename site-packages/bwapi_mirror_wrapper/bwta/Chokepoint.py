from bwta import Chokepoint as Java_Chokepoint

if False:
    import bwapi_mirror_wrapper.bwapi.Position as Position
    import bwapi_mirror_wrapper.bwapi.Pair as Pair
    class Chokepoint(object):

        def __init__(self, field):
            self._field = field

        def __getattribute__(self, attr):
            field_name = object.__getattribute__(self, "_field")
            unit_type = getattr(Java_Chokepoint, field_name)
            return getattr(unit_type, attr)

        def getRegions(self, ):
            return Pair.Pair()
        
        def getSides(self, ):
            return Pair.Pair()
        
        def getCenter(self, ):
            return Position.Position()
        
        def getWidth(self, ):
            pass
        
else:
    Chokepoint = Java_Chokepoint
